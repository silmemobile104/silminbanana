# DESIGN.md — ระบบดีไซน์ Silmin Banana

เอกสารนี้คือแหล่งอ้างอิงเดียวของงานภาพทั้งระบบ ค่าทุกตัวที่เขียนไว้ที่นี่ถูกประกาศจริงเป็น
token ใน `public/css/styles.css` — ถ้าจะเปลี่ยนสี ระยะ หรือรูปทรงใด ให้แก้ที่ token
ไม่ใช่ที่หน้าใดหน้าหนึ่ง

> **หมายเหตุการเปลี่ยนแปลง** เอกสารฉบับก่อนหน้าอธิบายระบบพื้นดำ (`#000000`) ที่ใช้สีเน้น
> เดียวและห้ามใช้สีสื่อสถานะ ระบบนั้นถูกแทนที่ทั้งหมดด้วยระบบพื้นสว่างในเอกสารนี้
> ตามภาพต้นแบบที่กำหนดไว้

---

## 1. ภาพรวม

หน้าจอเป็น **เอกสารสว่างวางบนแถบนำทางสีเข้ม** — การกลับสีเพียงจุดเดียวนี้คือโครงสร้างของ
ผลิตภัณฑ์: แถบนำทางคือเฟอร์นิเจอร์ที่อยู่กับที่ ส่วนเนื้อหาคือกระดาษที่วางทับลงไป

นี่คือเครื่องมือทำงาน ไม่ใช่หน้าโฆษณา พนักงานสาขาเปิดค้างไว้ทั้งวันเพื่อกวาดสายตาหาตัวเลข
ที่ผิดปกติ ดังนั้นความหนาแน่นของข้อมูล ความสม่ำเสมอ และการอ่านออกจึงสำคัญกว่าการแสดงออก

**หลักสามข้อที่ใช้ตัดสินทุกกรณี**

1. **พื้นสว่าง แถบนำทางเข้ม** เนื้อหาอยู่บนการ์ดขาวบนผืนเทาอ่อน แถบนำทางเป็นเกาะสีเข้ม
2. **สีทำหน้าที่จำแนก ไม่ได้ทำหน้าที่ตกแต่ง** ทุกสีในระบบคือแผ่นไอคอนประจำหมวด สถานะ
   หรือลิงก์ ไม่มีสีใดถูกใส่เพื่อ "ให้ดูดี"
3. **เหลืองคือแบรนด์ ไม่ใช่ส่วนติดต่อผู้ใช้** เหลืองเติมโลโก้ ปุ่มหลัก แถบเมนูที่เลือกอยู่
   และเส้นกราฟยอดขาย แล้วหยุดแค่นั้น **ตัวอักษรบนเหลืองเป็น `#1d1d1f` เสมอ**
   (ขาวบน `#FFE169` ได้อัตราส่วน 1.29:1 — อ่านไม่ออก)

---

## 2. สี

### แบรนด์

| Token | ค่า | ใช้เมื่อ |
|---|---|---|
| `--primary` | `#FFE169` | พื้นที่เติมสี: ปุ่มหลัก โลโก้ แถบเมนูที่เลือก ป้ายนับในแถบนำทาง |
| `--primary-graphic` | `#F2C230` | เหลืองเดียวกันที่เข้มพอจะเป็นเส้นหนา 2px หรือไอคอน 16px บนพื้นขาว |
| `--primary-ink` | `#7A5A00` | เหลืองในรูปแบบที่ **ต้องอ่าน** — ไอคอนหัวการ์ด ข้อความเน้น (6.4:1 บนขาว) |
| `--primary-pressed` | `#F5D24E` | สถานะกด |
| `--primary-focus` | `#B8860B` | วงโฟกัสคีย์บอร์ด |
| `--primary-wash` | `#FFF8E1` | เงาโฟกัสของช่องกรอก |
| `--on-primary` | `#1d1d1f` | ตัวอักษรบนเหลือง — ไม่มีข้อยกเว้น |

`--accent-primary` / `--accent-gold` / `--accent-secondary` เป็นชื่อเดิมที่ `app.js`
ยังเขียนอยู่ ทั้งหมดชี้มาที่ `--primary-ink`

### พื้นผิว

| Token | ค่า | ใช้เมื่อ |
|---|---|---|
| `--canvas` | `#F6F6F7` | ผืนหลังของพื้นที่เนื้อหา |
| `--surface` | `#FFFFFF` | การ์ด ตาราง กล่องโต้ตอบ |
| `--surface-sunken` | `#FAFAFB` | พื้นผิวที่ซ้อนอยู่ในการ์ด หัวตาราง |
| `--surface-hover` | `#F4F4F5` | แถวที่เมาส์ชี้ ปุ่มโครงร่างที่ชี้ |

พื้นผิวซ้อนพื้นผิวให้ **ลดระดับลง** ไปที่ `--surface-sunken` และตัดเงาทิ้ง ความลึกถูกประกาศ
ครั้งเดียวต่อการ์ด ไม่ใช่ครั้งเดียวต่อกล่อง

### ตัวอักษร

| Token | ค่า | อัตราส่วนบนขาว |
|---|---|---|
| `--ink` | `#17171A` | 17.4:1 — หัวเรื่อง ตัวเลข เนื้อความ |
| `--ink-2` | `#3F3F46` | 10.4:1 — ป้ายกำกับที่ต้องเน้น ค่าในชิปส่วนหัว |
| `--body-muted` | `#6B6B73` | 5.28:1 — ป้ายกำกับ คำอธิบายรอง หัวคอลัมน์ |

**ไม่มีระดับที่จางกว่านี้** ระบบเดิมมีโทน `#6e6e73` สำหรับ "ตัวหนังสือเล็ก" ซึ่งได้ 3.32:1
และตกเกณฑ์ AA — นี่เป็นเครื่องมือที่คนอ่านทั้งวัน ทุกคู่สีจึงต้องผ่าน 4.5:1

### เส้นและความลึก

| Token | ค่า | ใช้เมื่อ |
|---|---|---|
| `--hairline` | `#E8E8EA` | ขอบการ์ด ขอบตาราง |
| `--hairline-strong` | `#DBDBDF` | ขอบช่องกรอก ขอบปุ่มโครงร่าง |
| `--divider-soft` | `#F0F0F2` | เส้นคั่นแถวในตาราง |
| `--shadow-card` | `0 1px 2px rgba(16,24,40,.04)` | เงาเดียวของการ์ด — ต่ำกว่าระดับที่สังเกตเห็น |
| `--shadow-pop` | ดู `styles.css` | เฉพาะสิ่งที่ลอยขึ้นมาจริง: กล่องโต้ตอบ แถบแจ้งเตือน การ์ดล็อกอิน |

**ไม่มี gradient ตกแต่ง** บนปุ่ม การ์ด พื้นหลัง หรือองค์ประกอบแบรนด์ใด ๆ

ข้อยกเว้นเดียวคือ **พื้นที่ใต้เส้นกราฟ** (เส้นยอดขาย, สปาร์กไลน์ในการ์ด KPI) ซึ่งไล่จากสีเส้น
จางลงจนใส เพื่อช่วยให้เห็นรูปร่างของกราฟชัดขึ้น ไม่ใช่การตกแต่ง — จุดสูงสุดของไล่สีอยู่ที่ระดับ
เส้น (~30% ความทึบ) แล้วจางเป็น 0% ก่อนถึงฐาน กราฟบน canvas (Chart.js) ต้องคำนวณไล่สีใหม่ทุก
เฟรมจาก `chart.chartArea` จริง เพราะไล่สีคงที่จะยืด/บิดผิดสัดส่วนทันทีที่ผืนกราฟเปลี่ยนขนาด

### สถานะ

| Token | ค่า | ความหมาย |
|---|---|---|
| `--positive` | `#15803D` | เพิ่มขึ้น สำเร็จ เชื่อมต่ออยู่ (4.6:1) |
| `--negative` | `#B3261E` | ลดลง ยกเลิก ล้มเหลว |
| `--link` | `#1A6DCB` | ลิงก์และตัวเลขที่กดเข้าไปดูต่อได้ (5.3:1) |

### แผ่นไอคอนประจำหมวด

สี่เหลี่ยมมนที่วางไอคอนไว้ข้างหน้าหัวข้อ แต่ละคู่ให้ไอคอนผ่าน 3:1 บนพื้นของตัวเอง

| Token | พื้น | ไอคอน |
|---|---|---|
| `--tile-violet-*` | `#EEEAFD` | `#6D45E8` |
| `--tile-green-*` | `#E6F7EC` | `#10924A` |
| `--tile-amber-*` | `#FDF2D9` | `#A9750A` |
| `--tile-orange-*` | `#FDEEE0` | `#B85F14` |
| `--tile-rose-*` | `#FDECEE` | `#C4364A` |
| `--tile-blue-*` | `#E8F1FD` | `#1F62D4` |
| `--tile-slate-*` | `#EEF0F3` | `#55606F` |

**สีของแผ่นต้องคงที่ต่อความหมาย** — ตรวจนับสต็อกเป็นชมพู รับสินค้าเป็นน้ำเงินเสมอ
ทั้งในรายการ "ต้องดำเนินการ" และในบันทึกกิจกรรม ถ้าสีเดียวกันหมายถึงคนละเรื่องในสองที่
สีนั้นก็เลิกจำแนกอะไรทั้งสิ้น

### แถบนำทาง (เกาะสีเข้ม)

| Token | ค่า | อัตราส่วนบน `--nav-bg` |
|---|---|---|
| `--nav-bg` | `#141416` | — |
| `--nav-ink` | `#FFFFFF` | 18.7:1 |
| `--nav-muted` | `#9A9AA2` | 6.7:1 — รายการเมนูที่ยังไม่ถูกเลือก |
| `--nav-section` | `#83838D` | 5.0:1 — ป้ายหัวกลุ่ม |
| `--nav-active` | `#26262B` | พื้นของรายการที่เลือกอยู่ |
| `--nav-hover` | `#1E1E22` | พื้นเมื่อเมาส์ชี้ |

### การ์ดกลับสี (`.card-invert`)

การ์ดยอดขายบนสุดคือตัวเลขพาดหัวของทั้งหน้า และได้พื้นเข้มแบบเดียวกับที่ปกหนังสือให้พาดหัว

`.card-invert` **ประกาศ token ของตัวเองใหม่ทั้งชุด** (`--ink`, `--body-muted`, `--surface`,
`--hairline`, `--positive`, `--negative`, `--link`) ทุกอย่างข้างในจึงตามระบบเดิมโดยอัตโนมัติ
— ป้ายกำกับ ตัวเลข ตัวเปรียบเทียบ แกนเวลา และ dropdown

> **ห้ามเขียน `style="color:white"` ทับเป็นรายชิ้น** วิธีนั้นทาสีเฉพาะกล่องนอก ส่วนลูกที่มี
> สีของตัวเอง (เช่น `.delta b` ที่เป็น `--body-muted`) ยังใช้สีเดิมและตกลงไปที่ 3.19:1
> บนพื้นเข้ม ถ้าต้องการการ์ดเข้มให้ใส่คลาส ไม่ใช่ทาสี

---

## 3. ตัวอักษร

**Sarabun** อย่างเดียว ฝังตัวเองในเครื่อง (`public/fonts/`) แบ่งตาม `unicode-range`
`@font-face` ถูก inline ไว้ใน `index.html` เพื่อให้เบราว์เซอร์เจอไฟล์ฟอนต์ตั้งแต่ parse
ครั้งแรก — ต้นทางคือ `public/css/sarabun.css` แก้แล้วต้องอัปเดตทั้งสองที่

| บทบาท | ขนาด | น้ำหนัก | ระยะอักษร |
|---|---|---|---|
| ตัวเลขพาดหัว (`.metric-hero`) | 46px | 800 | −0.045em |
| ตัวเลขหลัก (`.metric-value`) | 34px | 800 | −0.04em |
| หัวหน้าเพจ (`h1`) | 23px | 700 | −0.028em |
| วลีสถานะ (`.metric-phrase`) | 22px | 700 | −0.03em |
| หัวการ์ด (`.card-title`) | 15px | 700 | −0.02em |
| เนื้อความ (`body`) | 15px | 400 | −0.01em |
| ตาราง เมนู ปุ่ม | 13.5px | 400–600 | −0.005em |
| ป้ายกำกับ ชิป | 12–12.5px | 400–600 | 0 |
| ป้ายหัวกลุ่มเมนู | 11.5px | 600 | +0.01em |

**ตัวเลขทุกตัวที่เรียงเป็นคอลัมน์ต้องใช้ `font-variant-numeric: tabular-nums`** — ตาราง
ตัวชี้วัด เวลา ลำดับ ยอดเงิน ถ้าตัวเลขไม่ตรงหลักกัน สายตาจะกวาดหาค่าผิดปกติไม่ได้

---

## 4. ระยะและรูปทรง

ฐาน 4px: `--space-1` (4) ถึง `--space-9` (64) ระยะห่างระหว่างการ์ดคือ `--space-4` (16px)
ระยะในการ์ดคือ `--space-6` (24px)

| Token | ค่า | ใช้กับ |
|---|---|---|
| `--radius-xs` | 6px | รูปย่อสินค้า |
| `--radius-sm` | 8px | รายการเมนู ปุ่มเล็ก |
| `--radius-md` | 10px | ปุ่ม ช่องกรอก ชิปส่วนหัว |
| `--radius-lg` | 14px | แผ่นไอคอน แถวรายการ การ์ดซ้อน |
| `--radius-xl` | 16px | การ์ด ตาราง กล่องโต้ตอบ |
| `--radius-pill` | เต็ม | ป้ายสถานะ แถบแจ้งเตือน |

`--sidebar-w: 244px` · `--topbar-h: 76px` · `--content-max: 1500px` · `--tap: 40px`

---

## 5. องค์ประกอบ

### ปุ่ม

**หนึ่งการ์ดมีปุ่มทึบได้หนึ่งปุ่ม** ที่เหลือเป็นปุ่มโครงร่าง

- `.btn-primary` — เหลืองทึบ ตัวอักษร `--on-primary` ใช้กับการกระทำที่ปิดงาน
- `.btn-secondary` — ขาว ขอบ `--hairline-strong` ตัวอักษร `--ink` เป็นปุ่มที่ใช้บ่อยที่สุด
  ("ดูทั้งหมด", "เริ่มตรวจสอบสต็อก")
- `.btn-danger` / `.btn-red` — ขาว ตัวอักษร `--negative` ขอบแดงอ่อน อ่านออกว่าอันตราย
  แต่ไม่แย่งความเด่นจากปุ่มหลัก กล่องยืนยันยังเป็นด่านจริง
- `.link-quiet` — ข้อความล้วน `--body-muted` สำหรับ "ดูทั้งหมด" ที่ไม่ควรดังเท่าปุ่ม

### การ์ด

`--surface` + `--hairline` + `--radius-xl` + `--shadow-card` หัวการ์ดคือ `.card-head`:
ชื่อเรื่องอยู่ซ้าย กลุ่มควบคุมได้ไม่เกินหนึ่งชุดอยู่ขวา

### ป้ายสถานะ

ป้ายใช้สีจำแนก เพราะคนที่กวาดสายตาหาคอลัมน์ผลต่างต้องเห็นตัวที่ผิดปกติโดยไม่ต้องอ่านทีละคำ
ทุกคู่ผ่าน 4.5:1

`.badge-green` `.badge-blue` `.badge-purple` `.badge-yellow` `.badge-red` `.badge-gray`

### แถวรายการ

สามรูปแบบ ไวยากรณ์เดียว: **แผ่นไอคอน → ป้ายสองบรรทัด → ค่าท้ายแถว**

- `.action-row` — งานที่ต้องทำ (กดได้ มีลูกศรท้ายแถว)
- `.rank-row` — อันดับ (เลขลำดับ รูปย่อ ชื่อ จำนวนเป็นสีลิงก์)
- `.activity-row` — บันทึกกิจกรรม (เวลาอยู่ท้ายแถว)

### แดชบอร์ดผู้บริหาร

`.exec-grid` = คอลัมน์อ่านกว้าง + รางข้างกว้างคงที่ 330px รางกว้างเท่านี้เพราะหน้าที่ของมัน
(อะไรต้องจัดการ / เพิ่งเกิดอะไรขึ้น) ไม่ได้ดีขึ้นเมื่อมีที่มากกว่านี้

| คลาส | คืออะไร |
|---|---|
| `.kpi` | การ์ดตัวเลขประจำวัน — แผ่นไอคอน + ป้าย + ค่า + ส่วนต่าง + กราฟเล็ก + คำอธิบายท้าย |
| `.seg` / `.seg-btn` | ปุ่มสลับแบบแบ่งช่อง ปุ่มที่เลือกใช้ `aria-pressed="true"` และพื้นเหลือง |
| `.rank-chip` | ตัวเลขอันดับ สามอันดับแรกพื้นเหลือง ที่เหลือเป็นโครงร่างเทา |
| `.bar-track` / `.bar-fill` | แถบสัดส่วนในตาราง |
| `.alert` + `-critical/-warning/-pending/-clear` | แถวใน Attention Center |
| `.status-inline` + `.dot` | สถานะแบบบรรทัดเดียว |
| `.section-label` | หัวแผงภาษาไทย ระยะอักษรแคบกว่าตัวอักษรละตินทั่วไป (0.01em) เพราะสระ/วรรณยุกต์ไทยเลื่อนหลุดจากพยัญชนะเมื่อระยะห่างกว้างเกินไป |

**จุดพัง (breakpoint) มีเหตุผลของมัน** ต่ำกว่า 1560px การ์ดตัวเลขพับเป็น 2×2 และการ์ดคู่
ซ้อนกันแทนที่จะเรียงข้างกัน เพราะข้างรางกว้าง 330px นั้นไม่เหลือที่ให้ตารางหกคอลัมน์
— ป้ายที่ถูกตัดคำแย่กว่าบล็อกที่สูงขึ้น

### ข้อมูลจริงเท่านั้น

ตัวเลขทุกตัวบนแดชบอร์ดต้องมาจากเอกสารในฐานข้อมูล **ถ้าไม่มีข้อมูล ให้ตัดองค์ประกอบนั้นทิ้ง**
อย่าเติมค่าที่ดูสมเหตุสมผล

ตัวอย่างที่เกิดขึ้นจริงในหน้านี้:

- **ไม่มีเป้ายอดขายในโมเดล** → การ์ดยอดขายจึงไม่มีแถบความคืบหน้าและตัวเลข % ของเป้า
- **มูลค่าสต็อกไม่เคยถูกบันทึกย้อนหลัง** → การ์ดมูลค่าสต็อกจึงไม่มีตัวเลขเทียบกับสัปดาห์ก่อน
- **สาขาที่ยังไม่ส่งรายงานนับสต็อก ไม่มีค่าความแม่นยำ** → แสดง `—` ไม่ใช่ `0%`

### กราฟยอดขาย

วาดเป็น **SVG ฝังใน HTML ไม่ใช่ Chart.js** — เป็นสิ่งแรกที่ปรากฏบนหน้า ไม่มีแกน ไม่มี
tooltip ไม่มีการโต้ตอบ จึงไม่มีเหตุผลให้รอไลบรารี 70KB เส้นโค้งใช้ Catmull-Rom แปลงเป็น
bezier, `preserveAspectRatio="none"` ยืดตามความกว้างการ์ด และ `vector-effect` ตรึงเส้นไว้ที่ 2px

กราฟที่มีแกนและ tooltip จริง (แดชบอร์ดผู้บริหาร) ใช้ Chart.js ที่โหลดเมื่อต้องใช้
**canvas อ่าน CSS custom property ไม่ได้** จึงต้องส่งค่าผ่าน `token('--ชื่อ')` เสมอ

---

## 6. การเข้าถึง (บังคับ ไม่ใช่ทางเลือก)

- ทุกคู่สีข้อความผ่าน **WCAG AA** (4.5:1 / 3:1 สำหรับตัวใหญ่) — ตรวจด้วย
  `scratchpad/contrast.js` ที่วัดจากหน้าจริงทั้ง 20 หน้า
- วงโฟกัสมีแบบเดียว: `2px solid var(--primary-focus)` เว้นระยะ 2px และเป็น `--primary`
  เมื่ออยู่บนพื้นเข้ม
- ไอคอนตกแต่งทุกตัวต้องมี `aria-hidden="true"` บนตัว `<i>` เอง
- ทุก `<th>` ต้องมี `scope` ทุก `<input>` ต้องมีป้ายกำกับ ทุกปุ่มที่มีแต่ไอคอนต้องมี `aria-label`
- กล่องโต้ตอบ: `inert` เมื่อปิด คืนโฟกัสกลับที่เดิม ปิดด้วย Escape วนโฟกัสด้วย Tab
- เคารพ `prefers-reduced-motion`

---

## 7. เอกสารสำหรับพิมพ์

ใบเสร็จ ใบกำกับภาษี ใบโอนย้าย ใบสั่งซื้อ และรายงานการเงิน **เป็นดำบนขาวเสมอ** และไม่ยืม
ธีมของหน้าจอเลย

เอกสารสามฉบับถูกเขียนลงหน้าต่างใหม่ด้วย `document.write()` ซึ่ง**ไม่ได้โหลด `styles.css`**
ดังนั้น `var(--token)` ในนั้นจะไม่มีค่า — ประกาศสีเดี่ยว ๆ ยังตกกลับเป็นดำบนขาวได้
แต่ **`border: 1px solid var(--hairline)` เป็น shorthand ที่ใช้ไม่ได้ทั้งบรรทัด และเส้นตาราง
จะหายไปทั้งใบ**

**เขียนค่าสีเป็นตัวเลขตรง ๆ ในเทมเพลตพิมพ์เท่านั้น** (`#000` `#333` `#999` `#f2f2f2` `#fff`)
ห้ามใช้ token

---

## 8. ที่มาของแต่ละอย่าง

| สิ่งที่ต้องแก้ | ไฟล์ |
|---|---|
| สี ระยะ รูปทรง องค์ประกอบ | `public/css/styles.css` |
| โครงเปลือกแอป แถบนำทาง ส่วนหัว | `public/index.html` |
| เนื้อหาแต่ละหน้า | `public/js/app.js` (ฟังก์ชัน `render*View`) |
| ไอคอน | `public/css/fontawesome.min.css` (subset — ดู `CLAUDE.md`) |

เปลี่ยน CSS หรือ JS แล้ว **ต้องเลื่อน `?v=` ใน `index.html`** ทั้งสองที่

`app.js` ยังเขียนชื่อ token ชุดเดิมอยู่ราวสองพันจุด (`--text-muted`, `--bg-card`,
`--canvas-elevated` ฯลฯ) ทั้งหมดถูก map ไว้ที่ท้ายบล็อก `:root` — **อย่าลบ** เพราะนั่นคือ
กลไกที่ทำให้สไตล์ชีตเดียวเปลี่ยนหน้าตาได้ทั้ง 20 หน้าพร้อมกัน
